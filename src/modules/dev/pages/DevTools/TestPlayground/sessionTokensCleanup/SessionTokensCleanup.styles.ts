import { Space, Typography } from 'antd';
import styled from 'styled-components';

export const PageStack = styled(Space)`
  width: 100%;
`;

export const ContentStack = styled(Space)`
  width: 100%;
`;

export const WarningParagraph = styled(Typography.Paragraph)`
  margin-bottom: 8px;
`;

export const LastWarningParagraph = styled(Typography.Paragraph)`
  margin: 0;
`;
