/**
 * Shared Prettier settings that match the team's current style (single quotes, concise width).
 * @type {import("prettier").Config}
 */
const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
};

export default config;
